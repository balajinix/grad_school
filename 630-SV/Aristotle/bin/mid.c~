
/*---------------------------------------------------------------------------*/
/*   
		  Program file name : mid.c
		  Functions         : main
*/
/*---------------------------------------------------------------------------*/

/* include files */
#include<stdio.h>

/*---------------------------------------------------------------------------*/
/*   
		  Function         : main
		  Parameters       : none
		  Input            : integers a, b, c
		  Output           : middle value 
		  Description      : finds middle value of a, b, c 
*/
/*---------------------------------------------------------------------------*/


main() 
{
int x, y, z, m;  
printf("Enter the 3 numbers \n");
scanf("%d%d%d", &x,&y,&z);

   m = z;

  if(y < z)
  {
	if(x < y)
	   m = y;
    else if(x < z)
	   m = x; 
  }
  else 
	if(x > y)
	  m = y;
  else if(x > z)
	  m = x;
  
printf("Middle number is %d\n", m);

}
	  
	   
